@model BookingManagement.Services.DTOs.ChangePasswordDto

@{
    ViewData["Title"] = "Đổi mật khẩu";
}

<!-- Page Heading -->
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Đổi mật khẩu người dùng</h1>
</div>

<div class="row">
    <div class="col-lg-6">
        <div class="card shadow mb-4">
            <div class="card-header py-3 d-flex align-items-center">
                <h6 class="m-0 font-weight-bold text-primary">Nhập mật khẩu mới</h6>
            </div>
            <div class="card-body">
                <form asp-action="ChangePassword">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                    <input type="hidden" asp-for="UserId" />
                    
                    <div class="mb-4">
                        <label asp-for="NewPassword" class="form-label">Mật khẩu mới <span class="text-danger">*</span></label>
                        <input asp-for="NewPassword" type="password" class="form-control" placeholder="Nhập mật khẩu mới" />
                        <span asp-validation-for="NewPassword" class="text-danger"></span>
                        <div class="form-text">Mật khẩu phải có ít nhất 6 ký tự.</div>
                    </div>
                    
                    <div class="alert alert-warning">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        Lưu ý: Mật khẩu sẽ được thay đổi ngay lập tức sau khi lưu.
                    </div>
                    
                    <div class="d-flex justify-content-between mt-4">
                        <a asp-action="Details" asp-route-id="@Model.UserId" class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i> Quay lại
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-save me-1"></i> Lưu mật khẩu mới
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-lg-6">
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Hướng dẫn đặt mật khẩu</h6>
            </div>
            <div class="card-body">
                <p>Một mật khẩu tốt nên:</p>
                <ul>
                    <li>Có độ dài ít nhất 8 ký tự</li>
                    <li>Bao gồm cả chữ hoa và chữ thường</li>
                    <li>Bao gồm ít nhất một ký tự số</li>
                    <li>Bao gồm ít nhất một ký tự đặc biệt (!, @@, #, $, %, ^, &, *)</li>
                    <li>Không dễ đoán (tránh các từ thông dụng, ngày sinh, tên, etc.)</li>
                </ul>
                <div class="mt-3">
                    <p class="text-muted fst-italic">
                        <i class="bi bi-info-circle me-1"></i>
                        Trong môi trường thực tế, cần áp dụng các biện pháp bảo mật mạnh hơn như mã hóa mật khẩu và cưỡng chế chính sách mật khẩu phức tạp.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
